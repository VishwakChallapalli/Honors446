# Optional: provide an OpenAI API key to enable real LLM-backed responses.
# When this key is missing the agent automatically falls back to a deterministic
# rule-based engine so the application keeps working offline.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
FLASK_DEBUG=1
